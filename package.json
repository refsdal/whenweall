{
  "name": "whenweall-e2e",
  "private": true,
  "type": "module",
  "comment": "This root package.json exists ONLY for Playwright's e2e suite (e2e/, playwright.config.ts) — the app itself lives in web/ (the SPA) and cmd/+internal/ (the Go backend), each with its own toolchain. Do not add application dependencies here.",
  "scripts": {
    "test:e2e": "playwright test",
    "screenshots": "SCREENSHOTS=1 playwright test --grep @screenshots"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "@types/node": "^26.2.0"
  }
}
